# T056.3 Rework Candidate Flow Check

## Goal

验证一次返工候选流程，证明 rework candidate 可以被安全识别、检查，并进入 confirmed execution stub。

## Candidate

| 字段 | 值 |
|------|---|
| task_id | G007 |
| round | 1 |
| source | Tester FAIL（模拟） |
| reason | 验证 rework candidate flow |
| candidate_status | found |
| prompt_exists | true |

选择 G007-R1 的原因：
- G007 已有完整闭环（COMPLETE），不会触发真实返工需求
- rework_prompt.md 已存在
- 只用于验证 candidate flow 链路

## Dry-run Flow

命令：

```bash
python runner.py execute-rework --project projects/down-100-floors-game --task G007 --round 1 --confirm "APPROVE_REWORK task=G007 round=1"
```

结果：

```
Status：READY_TO_EXECUTE
Dry Run：True
Report：projects/down-100-floors-game/reports/rework/G007-R1-execution-check.md
```

判定：PASS

## Confirmed Stub Flow

命令：

```bash
python runner.py execute-rework --project projects/down-100-floors-game --task G007 --round 1 --confirm "APPROVE_REWORK task=G007 round=1" --real-execution
```

结果：

```
execution_allowed=True
real_execution_requested=True
real_execution_performed=False
EXECUTION_MODE=confirmed_rework_execution_stub
CHECK_RESULT=pass
confirmation_status=confirmed
round_status=valid
NEXT_ACTION=ready_for_T056.3_candidate_flow_validation
```

判定：PASS

## Safety Result

| 检查项 | 结果 |
|--------|------|
| 是否调用 Claude Code | no |
| 是否修改业务代码 | no |
| real_execution_performed | false |
| 是否进入 full loop | no |
| 业务代码文件变更 | 无（index.html / style.css / script.js 均未变） |

## Check Result

pass

## Next

T056.4 设计 full loop resume
