# down-100-floors-game 工作流说明

## 项目定位

本项目是 `multi-agent-runner` 的第一个业务验证项目。

## 当前阶段

只验证 Web MVP。

## 后续阶段

后续再验证平台适配和角色系统。

## 工作流原则

1. 发现框架问题时，应回写 `multi-agent-runner` 主项目
2. 可复用经验应记录到主项目 `memory/` 或模板中
3. 本项目的每个开发任务都应有开发报告
4. 本项目的测试和审查报告应按 `multi-agent-runner` 的协议格式输出
